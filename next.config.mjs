/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // If you ever add external images, configure remotePatterns here.
};

export default nextConfig;
